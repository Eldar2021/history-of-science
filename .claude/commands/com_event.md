Draft one or more timeline events for Uchkun following the content template. Arguments: $ARGUMENTS (event names or years; if empty, pick the next unwritten events from the core list in `doc/03-icerik-stratejisi.md`, highest importance first).

1. Read `doc/03-icerik-stratejisi.md` in full: eras, disciplines, the event template, the writing rules (voice, no formulas, one analogy, honesty about uncertainty, no lone-hero narrative, Central Asia emphasis), and the core event list with its years/precision/importance.
2. For each event, research with web search: find at least 3 sources (prefer Britannica, Stanford Encyclopedia of Philosophy, English Wikipedia, university pages). Note any disagreement on the year and set precision accordingly (`exact` / `circa` / `decade` / `century`). Mark legendary anecdotes as legends.
3. Write the event in English (the source language of the content pipeline) unless the user asks for Kyrgyz or Turkish. Fields: year, year_end (optional), precision, importance (1-5), slug (English, kebab-case), title (≤80 chars), summary (1-2 sentences, ≤200 chars), body (300-600 words, markdown, structured as: scene / what happened / why it was hard and what it needed / what it opened), why_it_matters (2-3 sentences), if_you_were_there (1-2 sentences about what people did NOT know then), disciplines (1-3 slugs), people (names + roles), builds_on (earlier event slugs from the core list), enables (later event slugs), sources (title + URL + kind).
4. Output each event as a JSON object matching those field names, followed by a 2-3 line note in Turkish listing anything the user should verify (uncertain year, competing priority claims, a fact you could not confirm with 2 sources).
5. If `backend/seed/` or an admin API exists, ask whether to insert as `status='draft'`; never insert as `published`.
